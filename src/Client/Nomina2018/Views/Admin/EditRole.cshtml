@model EditRoleViewModel

@{
    ViewBag.Title = "Editar Role";
    ViewBag.menu_title = "Administrar";
    ViewBag.sub_menu_title = "Roles";
}



<form method="post" class="mt-3">
    <div class="form-group row">
        <label asp-for="Id" class="col-sm-2 col-form-label"></label>
        <div class="col-sm-10">
            <input asp-for="Id" disabled class="form-control form-control-sm">
        </div>
    </div>
    <div class="form-group row">
        <label  class="col-sm-2 col-form-label">Role</label>
        <div class="col-sm-10">
            <input asp-for="RoleName" class="form-control form-control-sm">
            <span asp-validation-for="RoleName" class="text-danger"></span>
        </div>
    </div>

    <div asp-validation-summary="All" class="text-danger"></div>

    <div class="form-group row">
        <div class="col-sm-10">
            <button type="submit" class="btn btn-primary btn-sm">Actualizar</button>
            <a asp-action="ListRoles" class="btn btn-primary btn-sm">Cancelar</a>
        </div>
    </div>

   
        @*<div class="">
            <h4>Usuarios en Rol</h4>
        </div>
    


            <table class="table">
                <tbody>
                 
                        @if (Model.Users.Any())
                        {
                            foreach (var user in Model.Users)
                            {
                              
                    <tr>
                        <td>@user</td>
                    </tr>
                            }
                        }
                        else
                        {
                            
                            <tr>
                                <td>Ninguno por el momento</td>
                            </tr>
                        }
                    
                </tbody>

            </table>*@
 

        @*<div class="form-group row">
            <div class="col-sm-10">
                <a asp-controller="Admin" asp-action="EditUsersInRole"
                   asp-route-roleId="@Model.Id" class="btn btn-primary btn-sm">
                    Agregar 
                </a>
            </div>
        </div>*@

</form>